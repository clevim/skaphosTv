diff --git a/node_modules/react-native-video/android/src/main/java/com/brentvatne/common/api/VideoTrack.kt b/node_modules/react-native-video/android/src/main/java/com/brentvatne/common/api/VideoTrack.kt
index 60e5da2..ac838c7 100644
--- a/node_modules/react-native-video/android/src/main/java/com/brentvatne/common/api/VideoTrack.kt
+++ b/node_modules/react-native-video/android/src/main/java/com/brentvatne/common/api/VideoTrack.kt
@@ -8,6 +8,9 @@ class VideoTrack {
     var width = 0
     var height = 0
     var bitrate = 0
+    // fps declarado pelo container. 0 quando desconhecido (típico de MKV/TS) —
+    // usado pelo app para casar a taxa de atualização da TV com o conteúdo.
+    var frameRate = 0f
     var codecs = ""
     var id = -1
     var trackId = ""
diff --git a/node_modules/react-native-video/android/src/main/java/com/brentvatne/common/react/VideoEventEmitter.java b/node_modules/react-native-video/android/src/main/java/com/brentvatne/common/react/VideoEventEmitter.java
index d87b6d8..dc14e04 100644
--- a/node_modules/react-native-video/android/src/main/java/com/brentvatne/common/react/VideoEventEmitter.java
+++ b/node_modules/react-native-video/android/src/main/java/com/brentvatne/common/react/VideoEventEmitter.java
@@ -215,6 +215,7 @@ public class VideoEventEmitter {
                 videoTrack.putInt("width", vTrack.getWidth());
                 videoTrack.putInt("height",vTrack.getHeight());
                 videoTrack.putInt("bitrate", vTrack.getBitrate());
+                videoTrack.putDouble("frameRate", vTrack.getFrameRate());
                 videoTrack.putString("codecs", vTrack.getCodecs());
                 videoTrack.putInt("trackId",vTrack.getId());
                 videoTrack.putBoolean("selected", vTrack.isSelected());
diff --git a/node_modules/react-native-video/android/src/main/java/com/brentvatne/exoplayer/ReactExoplayerView.java b/node_modules/react-native-video/android/src/main/java/com/brentvatne/exoplayer/ReactExoplayerView.java
index bb09f1a..8d4e0b0 100644
--- a/node_modules/react-native-video/android/src/main/java/com/brentvatne/exoplayer/ReactExoplayerView.java
+++ b/node_modules/react-native-video/android/src/main/java/com/brentvatne/exoplayer/ReactExoplayerView.java
@@ -1234,6 +1234,7 @@ public class ReactExoplayerView extends FrameLayout implements
         videoTrack.setWidth(format.width == Format.NO_VALUE ? 0 : format.width);
         videoTrack.setHeight(format.height == Format.NO_VALUE ? 0 : format.height);
         videoTrack.setBitrate(format.bitrate == Format.NO_VALUE ? 0 : format.bitrate);
+        videoTrack.setFrameRate(format.frameRate == Format.NO_VALUE ? 0 : format.frameRate);
         if (format.codecs != null) videoTrack.setCodecs(format.codecs);
         videoTrack.setTrackId(format.id == null ? String.valueOf(trackIndex) : format.id);;
         return videoTrack;
@@ -1658,7 +1659,7 @@ public class ReactExoplayerView extends FrameLayout implements
             disableTrack(rendererIndex);
             return;
         } else if (type.equals("language")) {
-            for (int i = 0; i < groups.length; ++i) {
+            if (value != null) for (int i = 0; i < groups.length; ++i) {
                 Format format = groups.get(i).getFormat(0);
                 if (format.language != null && format.language.equals(value.asString())) {
                     groupIndex = i;
@@ -1666,7 +1667,7 @@ public class ReactExoplayerView extends FrameLayout implements
                 }
             }
         } else if (type.equals("title")) {
-            for (int i = 0; i < groups.length; ++i) {
+            if (value != null) for (int i = 0; i < groups.length; ++i) {
                 Format format = groups.get(i).getFormat(0);
                 if (format.id != null && format.id.equals(value.asString())) {
                     groupIndex = i;
@@ -1674,10 +1675,21 @@ public class ReactExoplayerView extends FrameLayout implements
                 }
             }
         } else if (type.equals("index")) {
-            if (value.asInt() < groups.length) {
-                groupIndex = value.asInt();
+            if (value != null) {
+                try {
+                    // JS envia números como ReadableType.Number (double) pelo bridge — usar asDouble()
+                    int idx = (int) value.asDouble();
+                    if (idx >= 0 && idx < groups.length) groupIndex = idx;
+                } catch (Exception e) {
+                    // fallback: tenta asInt() para casos onde o bridge serializa como Int
+                    try {
+                        int idx = value.asInt();
+                        if (idx >= 0 && idx < groups.length) groupIndex = idx;
+                    } catch (Exception ignored) {}
+                }
             }
         } else if (type.equals("resolution")) {
+            if (value == null) return;
             int height = value.asInt();
             for (int i = 0; i < groups.length; ++i) { // Search for the exact height
                 TrackGroup group = groups.get(i);
