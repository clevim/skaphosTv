services:
  proxy:
    build:
      context: .
      dockerfile: docker/Dockerfile.proxy
    restart: unless-stopped
    expose:
      - "3001"

  web:
    build:
      context: .
      dockerfile: docker/Dockerfile.web
    restart: unless-stopped
    ports:
      - "8080:80"
    depends_on:
      - proxy
